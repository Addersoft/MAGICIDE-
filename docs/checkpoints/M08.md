CHECKPOINT
Milestone: M08 v0.8.0 — rune queue and Kenaz hitscan proof.
Status: generated/reviewed; headless tests to be recorded in TEST_RESULTS.
Implemented: data-driven Kenaz resource, 3-slot queue, key 1 insert, C clear, empty-queue Tag,
Kenaz-only scaled hitscan, unsupported-mix reject, HUD + overhead cubes.
Files changed: rune scripts/data, combat/input/HUD, player/main scenes, project input map, tests/docs.
Important architecture: SpellCatalog is pure data; RuneQueue owns slots; Combat still owns the ray.
Known bugs: Gate A still pending; Kenaz is not a fire volume yet.
Tests performed: see TEST_RESULTS.md after the M08 runner.
Next task: M09 bind a second rune (Isa) or broom proof after Kenaz feel is accepted.
Do not change: FPS on foot, Tag numbers, round/respawn owners, no silent 24-rune dump.
Technical debt: only Kenaz bound; hitscan stands in for fire; Gate A/export still open.
